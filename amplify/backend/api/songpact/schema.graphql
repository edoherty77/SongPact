type User @model {
  _id: ID!
  firstName: String!
  lastName: String!
  email: String!
  artistName: String!
  contacts: [User]
  pacts: [Pact]
}

type Pact @model @auth(rules: [{ allow: public }]) {
  id: ID!
  type: String!
  recordTitle: String!
  role: String
  initBy: String
  contributors: [User]
  status: Boolean
}
