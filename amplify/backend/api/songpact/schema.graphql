type User @model {
  _id: ID!
  firstName: String!
  lastName: String!
  email: String!
  artistName: String!
  contacts: [User]
  pacts: [Pact]
}

type Pact @model @auth(rules: [{ allow: public }]) {
  id: ID!
  type: String!
  recordTitle: String!
  role: String
  initBy: String
  contributors: [User]
  perfPublish: Int
  prodAdvance: Int
  prodPublish: Int
  prodRoyalty: Int
  prodCredit: String
  status: Boolean
}
